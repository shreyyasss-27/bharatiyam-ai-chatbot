version: '3.8'

services:
  chatbot:
    build: .
    ports:
      - "8000:8000"
    env:
      - MONGO_URI=${MONGO_URI}
      - DB_NAME=${DB_NAME}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - GROQ_MODEL=${GROQ_MODEL}
      - EMBED_MODEL=${EMBED_MODEL}
    restart: unless-stopped
